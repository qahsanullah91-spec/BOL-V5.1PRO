const fs=require('fs');const s=fs.readFileSync('components/accounts/account-manager.tsx','utf8');const pos=92409;let line=0;let col=0;let acc=0;const lines=s.split(/\r?\n/);let cnt=0;let charCount=0;for(let i=0;i<lines.length;i++){charCount+=lines[i].length+1; if(charCount>pos){line=i+1; col=lines[i].length - (charCount-pos) +1; break;}}console.log('line',line,'col',col);const startLine=Math.max(0,line-6);const endLine=Math.min(lines.length,line+6);for(let i=startLine;i<endLine;i++){console.log((i+1).toString().padStart(5),lines[i]);}console.log('---');console.log('full line:',lines[line-1]);